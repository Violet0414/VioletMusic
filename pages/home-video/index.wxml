<!--pages/home-video/index.wxml-->
<view class="video">
  <view class="item" wx:for="{{topMV}}" wx:key="id">
    <vedio-item1 item="{{item}}" 
    bindtap="itemClick"
    data-item="{{item}}"></vedio-item1>
  </view>>
</view>
